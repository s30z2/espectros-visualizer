# Temp intermediate files from MoviePy
*TEMP_MPY_wvf_snd.*

# MP4s: key ones tracked via Git LFS, others regenerate locally
# (see .gitattributes — only whitelisted files added to repo)

# Batch pipeline artifacts
cache/*.json
output/*.mp4
output/*.raw.mp4
input/*.mp3
input/*.wav

# Blender intermediate frames (regenerate via `blender --background ... --python render_scene.py`)
blender/renders/*.png
blender/template_scene.blend
blender/template_scene.blend1

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Secrets — NEVER commit these
.env
.env.*
!.env.example
*_api_key*
secrets.json

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Claude Code session state
.claude/

# Large intermediate files (keep only the essentials)
# (keeping audio wav for reproducibility)
